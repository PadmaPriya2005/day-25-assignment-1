#include<stdio.h>
#include<conio.h>
main()
{
	int a[10],i,n,sum=0;
	printf(" Enter the n values :");
	scanf("%d",&n);
	printf(" Enter the array elements:");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	for(i=0;i<n;i++)
	{
		sum=sum+a[i];
	}
	printf(" Sum of array is %d",sum);
}
